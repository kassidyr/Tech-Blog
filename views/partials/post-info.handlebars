<article class="post">
    <div class="title">
        <h1>{{title}}</h1>
        <p>{{post_content}}</p>
    </div>
    <div class="meta">
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
    </div>
</article>